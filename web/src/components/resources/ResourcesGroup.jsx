/* eslint-disable */
import React from 'react';

import ResourceCard from './ResourceCard';
import GroupTitle from './GroupTitle';

const ResourceGroup = () => {
  return (
    <section>
      <GroupTitle title="Section Title" />
      <ResourceCard />
    </section>
  );
};

export default ResourceGroup;
